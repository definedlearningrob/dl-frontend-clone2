import SharedMainContent from '@dc/shared/MainContent/MainContent';
import { CreateOrUpdateBadge } from '@dc/components/Admin/BadgeManagement/CreateOrUpdateBadge/CreateOrUpdateBadge';

export const BadgeFormView = () => (
  <SharedMainContent>
    <CreateOrUpdateBadge />
  </SharedMainContent>
);
